{% extends 'base.html.twig' %}

{% block title %}Edit Skin{% endblock %}

{% block body %}
    <h1>Edit Skin</h1>

    {{ include('skin/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_skin_index') }}">Back to List</a>

    {{ include('skin/_delete_form.html.twig') }}
{% endblock %}
