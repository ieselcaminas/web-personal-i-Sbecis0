<form method="post" action="{{ path('app_skin_delete', {'id': skin.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ skin.id) }}">
    <button class="btn">Delete</button>
</form>
